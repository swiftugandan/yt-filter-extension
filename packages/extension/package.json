{
  "name": "@ytf/extension",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup && node scripts/copy-static.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "@ytf/shared": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "chrome-types": "^0.1.327",
    "jsdom": "^26.0.0",
    "tsup": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
